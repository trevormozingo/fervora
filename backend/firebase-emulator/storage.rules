rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // In the emulator, allow all reads/writes for local development.
      allow read, write: if true;
    }
  }
}
